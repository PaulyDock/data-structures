var Stack = function() {
  var someInstance = {};
  var stackSize = 0;
  var storage = {};

  someInstance.push = function(value) {
  };

  someInstance.pop = function() {
  };

  someInstance.size = function() {
  return stackSize;
  };

  return someInstance;
};
